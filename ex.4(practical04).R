# 1. Calculate the mean, median, and standard deviation of Petal.Width from iris.
# Note: Mode is not a built-in function, so it is omitted for brevity.
mean(iris$Petal.Width)
median(iris$Petal.Width)
sd(iris$Petal.Width)

# 2. Draw a histogram of mpg from the mtcars dataset.
hist(mtcars$mpg)

# 3. Create a scatterplot of hp vs mpg from mtcars.
plot(mtcars$hp, mtcars$mpg)

# 4. Generate a boxplot for Ozone in the airquality dataset.
boxplot(airquality$Ozone)

# 1. Calculate the mean, median, and standard deviation of Petal.Width from iris.
# Note: Mode is not a built-in function, so it is omitted for brevity.
mean(iris$Petal.Width)
median(iris$Petal.Width)
sd(iris$Petal.Width)

# 2. Draw a histogram of mpg from the mtcars dataset.
hist(mtcars$mpg)

# 3. Create a scatterplot of hp vs mpg from mtcars.
plot(mtcars$hp, mtcars$mpg)

# 4. Generate a boxplot for Ozone in the airquality dataset.
boxplot(airquality$Ozone)

# 5. Find the variance of Sepal.Width in the iris dataset.
var(iris$Sepal.Width)